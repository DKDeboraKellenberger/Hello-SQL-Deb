-- Obtiene todos los datos de la tabla "users" con nombre igual a brais y sara
-- El IN vendría a ser como un condicional en el que especificamos qué es lo que debe estar para que se cumpla la condición
SELECT * FROM users WHERE name IN('brais','sara');
